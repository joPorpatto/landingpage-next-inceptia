"use client";

import { Fade, Link, Paper, Typography } from "@mui/material";
import React, { useState } from "react";
import NextLink from "next/link";

const Page = () => {
  return (
    <Paper
      sx={{
        width: "100%",
        display: "flex",
        justifyContent: "center",
        flexDirection: "column",
      }}
    ></Paper>
  );
};

export default Page;
